# Django -> Backend
Django==3.2.10

# React -> Frontend
# Note: React is usually managed with npm or yarn, so it's not listed in requirements.txt

# Postgres connector
psycopg2-binary==2.9.3

# API testing
Insomnia==2021.5.3

# Django Rest Framework
djangorestframework==3.12.4
django-filter==2.4.0

# Python Pillow
Pillow==8.3.2

# Django Nested Routers
django-nested-routers==0.91.6

# Pytest for testing
pytest-django==4.4.0
